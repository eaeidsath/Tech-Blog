<div class="comment col-md-7">
    {{!-- {{#each comment.users as |user|}}
    <p>{{user.name}}</p>
    {{/each}} --}}
    <p>
        {{user.name}} on {{format_date date_created}}
    </p>
    <p>
        {{comment_body}}
    </p>
</div>